services:
  - type: web
    name: api-services-mvp
    env: node
    rootDir: agent_b/mvp_templates/api-services
    buildCommand: npm install && npm run build
    startCommand: npm start
    plan: starter
    envVars:
      - key: NODE_ENV
        value: production

